---
version: 1
slug: "src-features-letters-components-letters-shell-tsx"
primary_target: "apps/app/src/features/letters/components/letters-shell.tsx"
related_targets: ["apps/app/src/features/letters/components/letter-composer.tsx","apps/app/src/features/letters/components/letter-arrival-card.tsx"]
---

# Surface: /letters (shelf + reader + arrival) and /letters/compose

Mode: Operate (shelf) with a Read moment (reader). Visitor = the Reflective Writer
checking what travels toward their future self.

Job: see every letter's state in one glance (sealed / arrived / opened), open what
has arrived, compose new ones. Behavior frozen: server-enforced sealed lock, delete
anywhere, paywall gates, ZK decrypt-on-mount states.

Direction (user-locked): "Full codex letters." Letters render as ledger-ruled paper
slips (the editor's .ledger-rules), folio numerals per row, a circular gold wax-seal
disc marking sealed letters, gold 2px top hairline (reflection-sheet idiom) marking
arrived-unread, and the reader opens as a real letter page (ruled lines, drop cap,
hand byline). Signature moment: the wax seal breaks (one motion beat, reduced-motion
instant) when a delivered letter is first revealed. Composer joins via the same
ledger-rules page ground as the journal editor.

Constraints: tokens only, gold <=5% of any screen, no colored left borders above
1px, flat cards, motion library only, behavior and copy logic untouched.
Unresolved: none.
